import { InlineEdit } from '.';

// TODO:

export const Fries = () => {
	return (
		<>
			<InlineEdit container={{}} input={{ _type: 'text' }} preview={{}} />;
			<InlineEdit container={{}} input={{ _type: 'textarea' }} preview={{}} />;
		</>
	);
};
